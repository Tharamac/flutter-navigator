import 'package:flutter/material.dart';

class HelloPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("555"),),
      body: Center(child: Text("Hellooooooooooo Wooooooooorld Hamburger")),
      
    );
  }
}